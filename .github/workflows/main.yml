name: CI

on:
  push:
    branches:
      - "*"
  pull_request:
    branches: [ all ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Install dependencies
    - run: yarn install

    - name: Run tests
      run: yarn test
